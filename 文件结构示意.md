项目可以简化为以下几部分：

```text
/home/isee-cdh/ws/
├── Co-NavGPT2/          项目主体
├── conav_scripts/       实机启动、检查和停车脚本
├── FAST_LIO_ROS2/       雷达与 IMU 定位算法
├── Livox-SDK2/          Livox 雷达厂商通信 SDK
├── habitat-lab/         仿真任务与评测框架
├── habitat-sim/         三维仿真环境
├── build*/              ROS 编译中间文件
├── install*/            ROS 编译安装结果
└── log*/                构建和运行日志
```

## 1. `Co-NavGPT2/`

项目的主要源代码目录。

```text
Co-NavGPT2/
├── co_nav2_nav/         当前实际使用的 ROS 2/Nav2 导航代码
├── agents/              原始 Co-NavGPT2 智能体代码
├── utils/               原始感知、建图和规划工具
├── configs/             Habitat 仿真配置
├── multi-robot-setting/ 多机器人仿真环境修改
├── data/                本地仿真数据，不上传 Git
├── img/                 README 使用的图片
└── 若干根目录程序       仿真入口、旧实机入口和配准工具
```

### `co_nav2_nav/`

这是当前实机系统的核心 ROS 2 包。

```text
co_nav2_nav/
├── co_nav2_nav/       Python 节点和导航算法
├── launch/            各系统组件的启动文件
├── config/            机器人、SLAM 和 Nav2 参数
├── behavior_trees/    Nav2 行为树
├── rviz/              RViz 显示配置
├── test/              单元测试
└── ROS 包配置文件
```

其中最核心的两个程序是：

- `semantic_explorer.py`：接收 RGB-D、识别目标、计算目标地图坐标、选择探索位置并向 Nav2发送导航目标。
- `navigation_algorithms.py`：负责前沿搜索、坐标转换、目标接近位置和安全范围计算。

其他节点主要负责相机同步、FAST_LIO 坐标转换以及 ROS 2 接口适配。

### `agents/` 和 `utils/`

属于原始 Co-NavGPT2 论文实现，包含：

- YOLO-World 和 MobileSAM 目标识别；
- RGB-D 点云生成；
- 二维障碍物和已探索地图；
- 前沿检测；
- VLM 前沿分配；
- FMM 局部路径规划；
- 仿真和旧实机动作生成。

当前 Nav2 实机方案只复用了其中的部分设计，不以这些目录作为主运行入口。

### 根目录程序

- `main.py`、`main_vec.py`：原始 Habitat 仿真入口。
- `ros_multi_nav.py`、`ros_single_nav.py`：旧版实机导航入口。
- `multi_lidar_icp.py`：原始多机器人方案的雷达坐标配准。
- `README.md`：项目整体架构、安装和运行说明。

---

## 2. `conav_scripts/`

当前实机系统的运行管理脚本。

其中包含：

- 按顺序启动相机、小车、雷达、FAST_LIO、建图、Nav2 和识别节点；
- 检查话题、TF、里程计和节点状态；
- 人工授权目标识别测试；
- 取消导航、发送零速度并关闭节点；
- 启动 RViz 和状态监控。

正常实机操作应优先使用该目录中的脚本，而不是手工逐个执行旧版 Python 程序。

---

## 3. `FAST_LIO_ROS2/`

第三方雷达—IMU 紧耦合定位算法。

它接收 Livox 点云和 IMU，完成运动补偿、位姿估计和点云配准，并向本项目提供：

- 机器人里程计；
- 配准后的三维点云；
- FAST_LIO 坐标系关系。

本项目通常只需要配置 MID-360 参数和外参，不需要修改其核心算法。

---

## 4. 硬件厂商软件包

这些目录或 ROS 包负责与硬件直接通信，只列出最高一级名称：

- `ranger_bringup`：AgileX Ranger 小车驱动，负责 CAN 通信、底盘状态和速度控制。
- `realsense2_camera`：Intel RealSense 相机驱动，负责 RGB、深度图和相机参数。
- `livox_ros_driver2`：Livox MID-360 ROS 2 驱动，负责雷达点云和 IMU 数据。
- `Livox-SDK2`：Livox 官方底层通信 SDK。

这些驱动产生传感器数据或接收速度命令，本项目的识别、建图和决策代码不在这些目录中。

---

## 5. `habitat-lab/` 和 `habitat-sim/`

第三方仿真依赖：

- `habitat-sim`：加载三维场景、生成虚拟 RGB-D 图像并模拟机器人运动。
- `habitat-lab`：定义导航任务、组织数据集并计算评测指标。

它们只用于仿真实验，不参与当前真实小车运行。

---

## 6. 生成文件和本地数据

以下目录不是需要审阅的源代码：

- `data/`：HM3D 等仿真数据；
- `.venv/`：Python 虚拟环境；
- `build*/`：编译中间结果；
- `install*/`：ROS 安装结果；
- `log*/`：构建和运行日志；
- `.pt` 模型文件：YOLO-World 和 MobileSAM 权重。

这些内容应在本地下载或重新生成，不上传 GitHub。
