我按当前工作区的 [CoNav运行方法_FAST_LIO2版.md](/home/isee-cdh/ws/CoNav运行方法_FAST_LIO2版.md:658) 来解释。这里应区分两个版本：

- 当前新版：FAST_LIO2 + SLAM Toolbox + Nav2 + `semantic_explorer`
- 旧版：Co-NavGPT2 通过 ZMQ 发送速度，再由适配器转成 `/cmd_vel`

新版文档已经明确说明：不要同时运行旧版 `ros_single_nav.py` 和 ZMQ 适配器。

## 一、整体链路属于谁

```text
相机硬件
  → RealSense ROS 驱动
  → RGB、对齐深度、相机内参
  → Co-Nav2 语义识别
  → chair 的相机坐标
  → TF 坐标变换
  → chair 的 map 坐标

Livox 雷达和内置 IMU
  → Livox ROS 驱动
  → FAST_LIO
  → 三维定位、注册点云
  → FAST_LIO 里程计适配器
  → odom → base_link

注册点云
  → pointcloud_to_laserscan
  → /scan
  → SLAM Toolbox
  → /map、map → odom

Co-Nav2
  → /navigate_to_pose
  → Nav2
  → /cmd_vel
  → Ranger ROS 驱动
  → CAN
  → 小车电机

Ranger 状态
  → CAN
  → Ranger ROS 驱动
  → /odom、底盘状态
```

其中“连接它们的部分”主要包括：

- ROS 2 Topic：在节点之间传递数据
- TF：连接雷达、车体、相机、地图坐标系
- ROS 2 Action：Co-Nav2 向 Nav2 发送导航目标
- CAN：ROS 底盘节点与真实小车控制器之间通信
- Launch/YAML：决定启动哪些节点、话题名、坐标系和参数

---

## 二、逐条数据链路说明

### 1. `/livox/lidar`、`/livox/imu`

归属：

- 数据源属于 Livox MID-360 雷达
- 发布程序属于 Livox ROS 驱动
- 消费者是 FAST_LIO

原理：

- `/livox/lidar` 是激光点云，每个点包含空间位置、反射率、时间偏移等信息。
- `/livox/imu` 是角速度和线加速度。
- FAST_LIO 用 IMU 高频预测运动状态，再用当前点云和已有局部地图做配准修正。
- 这是一种紧耦合激光—惯性里程计，而不是单纯轮速里程计。

主要查看：

- 驱动启动入口：[msg_MID360_launch.py](/home/isee-cdh/ros2_ws/src/livox_ros_driver2/launch_ROS2/msg_MID360_launch.py)
- 驱动主程序：[livox_ros_driver2.cpp](/home/isee-cdh/ros2_ws/src/livox_ros_driver2/src/livox_ros_driver2.cpp:127)
- 消息组装和发布：[lddc.cpp](/home/isee-cdh/ros2_ws/src/livox_ros_driver2/src/lddc.cpp)
- FAST_LIO 订阅话题配置：[mid360.yaml](/home/isee-cdh/ws/FAST_LIO_ROS2/config/mid360.yaml:15)
- FAST_LIO 订阅入口：[laserMapping.cpp](/home/isee-cdh/ws/FAST_LIO_ROS2/src/laserMapping.cpp:809)

实现代码主要是 C++，配置和启动入口是 YAML、Python launch。

---

### 2. FAST_LIO 输出 `/Odometry`

归属：

- 属于 FAST_LIO 定位部分
- 输入来自雷达和 IMU
- 它不是 Ranger 底盘编码器 `/odom`

原理：

FAST_LIO 输出的是：

```text
frame_id       = camera_init
child_frame_id = body
```

含义是：估计 IMU/LiDAR 刚体 `body` 在 FAST_LIO 初始世界坐标系 `camera_init` 中的位姿。

主要代码：

- 核心状态估计、点云匹配和发布：[laserMapping.cpp](/home/isee-cdh/ws/FAST_LIO_ROS2/src/laserMapping.cpp:628)
- IMU 前向传播、去畸变：[IMU_Processing.hpp](/home/isee-cdh/ws/FAST_LIO_ROS2/src/IMU_Processing.hpp)
- 点云预处理：[preprocess.cpp](/home/isee-cdh/ws/FAST_LIO_ROS2/src/preprocess.cpp)
- 参数配置：[mid360.yaml](/home/isee-cdh/ws/FAST_LIO_ROS2/config/mid360.yaml:22)
- 启动入口：[mapping.launch.py](/home/isee-cdh/ws/FAST_LIO_ROS2/launch/mapping.launch.py:48)

`laserMapping.cpp` 中可直接看到：

- 订阅 `/livox/lidar`
- 订阅 `/livox/imu`
- 发布 `/Odometry`
- 发布 `/cloud_registered`
- 发布 `/cloud_registered_body`

这是查看 FAST_LIO 数据链路最关键的文件。

---

### 3. `/Odometry → /fastlio/odom → odom → base_link`

归属：

- 不属于雷达驱动
- 不属于底盘驱动
- 属于 Co-Nav2 工程中的“连接/适配层”

主要文件：

- [fastlio_odom_adapter.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/fastlio_odom_adapter.py:1)
- 参数：[robot.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/robot.yaml:1)
- 启动：[semantic_exploration.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/semantic_exploration.launch.py:28)

原理：

FAST_LIO 给出的是：

```text
T_camera_init_body
```

Nav2 需要的是：

```text
T_odom_base_link
```

由于雷达/IMU 的 `body` 和车体中心 `base_link` 并不重合，不能只改 frame 名。代码按照外参做刚体变换：

```text
T_odom_base =
T_camera_init_body × inverse(T_base_body)
```

具体实现位于：

- 外参配置：[fastlio_odom_adapter.py:27](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/fastlio_odom_adapter.py:27)
- 矩阵转换：[fastlio_odom_adapter.py:100](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/fastlio_odom_adapter.py:100)
- 发布 `/fastlio/odom`：[fastlio_odom_adapter.py:133](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/fastlio_odom_adapter.py:133)
- 发布 `odom → base_link` TF：[fastlio_odom_adapter.py:136](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/fastlio_odom_adapter.py:136)

这里的重点是：新版定位 TF 由 FAST_LIO 适配器发布，Ranger 必须设置 `publish_odom_tf:=false`，否则两个节点会争抢同一条 `odom → base_link`。

---

### 4. `/cloud_registered_body → /scan`

归属：

- `/cloud_registered_body` 是 FAST_LIO 的输出
- 转换过程属于雷达到二维建图的连接层
- 实现节点来自 `pointcloud_to_laserscan`

原理：

三维点云中只截取一定高度范围：

```text
0.05 m ≤ 高度 ≤ 1.50 m
```

然后将每个点投影到水平面，根据其方位角放进对应 LaserScan 角度格，通常每个角度保留最近距离，得到二维 `/scan`。

主要查看：

- 链路和参数：[fastlio_mapping_2d.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/fastlio_mapping_2d.launch.py:12)

关键配置包括：

- 输入 `/cloud_registered_body`
- 输出 `/scan`
- 目标坐标系 `base_link`
- 高度范围
- 角度范围
- 距离范围

具体投影算法属于系统安装的 `pointcloud_to_laserscan` ROS 包，不在 Co-Nav2 自己的 Python 文件中。

---

### 5. `/scan → SLAM Toolbox → /map + map → odom`

归属：

- 属于定位建图部分
- 不是 Co-Nav2 语义算法本身
- 是雷达与 Nav2 之间的地图连接层

原理：

SLAM Toolbox 接收二维激光扫描和当前 `odom → base_link`，通过扫描匹配估计地图坐标系中的机器人位置：

- `/map`：二维 OccupancyGrid，占据栅格地图
- `map → odom`：对连续里程计漂移进行全局修正

因此最终机器人全局位姿是：

```text
map → odom × odom → base_link
```

主要查看：

- 启动节点：[fastlio_mapping_2d.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/fastlio_mapping_2d.launch.py:36)
- SLAM 参数：[slam_toolbox.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/slam_toolbox.yaml:1)

这里要特别注意：

- `odom → base_link` 由 FAST_LIO adapter 发布
- `map → odom` 由 SLAM Toolbox 发布
- 同一条 TF 不应有两个发布者

---

### 6. 相机三个主要话题

```text
/camera/color/image_raw
/camera/aligned_depth_to_color/image_raw
/camera/color/camera_info
```

归属：

- 数据源属于 RealSense 相机
- 话题发布属于 `realsense2_camera` 驱动
- 使用者属于 Co-Nav2 感知模块

三者含义：

- `color/image_raw`：RGB 彩色图像
- `aligned_depth_to_color/image_raw`：已经投影到彩色相机像素坐标的深度图
- `color/camera_info`：相机内参 `fx、fy、cx、cy`

深度必须与 RGB 对齐，否则 RGB 检出的椅子像素不能正确查到对应深度。

主要查看：

- 启动参数：[rs_launch.py](/home/isee-cdh/rs515/ros2_ws/src/realsense-ros/realsense2_camera/launch/rs_launch.py)
- 图像、相机内参、对齐深度发布：[base_realsense_node.cpp](/home/isee-cdh/rs515/ros2_ws/src/realsense-ros/realsense2_camera/src/base_realsense_node.cpp:729)
- 对齐深度 frame 设置：[base_realsense_node.cpp](/home/isee-cdh/rs515/ros2_ws/src/realsense-ros/realsense2_camera/src/base_realsense_node.cpp:999)

这是 C++ 驱动实现；实际运行时主要通过 launch 参数控制分辨率、同步和深度对齐。

---

### 7. RGB + Depth → chair 检测

归属：

- 属于 Co-Nav2 感知部分
- 实现在 `semantic_explorer.py`
- YOLO 和 MobileSAM 是其调用的视觉模型

主要入口：

[semantic_explorer.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:59)

处理过程：

1. 用 `ApproximateTimeSynchronizer` 对 RGB 和深度进行近似时间同步。
2. YOLO-World 在 RGB 中检测 `chair`，得到检测框。
3. MobileSAM 根据检测框生成更精确的目标掩膜。
4. 在掩膜内筛选有效深度。
5. 使用深度中位数减少背景泄漏和异常点影响。
6. 利用相机内参反投影成三维相机坐标：

```text
X = (u - cx) × Z / fx
Y = (v - cy) × Z / fy
Z = depth
```

7. 通过 TF 将三维点从 `camera_color_optical_frame` 转到 `map`。
8. 连续多帧空间位置一致后确认目标。
9. 发布 `/semantic_explorer/markers`。

关键代码位置：

- 相机话题和同步：[semantic_explorer.py:132](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:132)
- 模型加载：[semantic_explorer.py:598](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:598)
- YOLO、SAM、深度计算：[semantic_explorer.py:629](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:629)
- 内参反投影：[semantic_explorer.py:663](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:663)
- 相机坐标转 map 坐标：[semantic_explorer.py:667](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:667)
- 多帧确认：[semantic_explorer.py:679](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:679)
- 发布目标 Marker：[semantic_explorer.py:746](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:746)

---

### 8. 相机 TF 链路

```text
base_link
  → camera_link
  → camera_color_optical_frame
```

归属：

- `base_link → camera_link` 属于相机和底盘之间的连接层，即相机安装外参
- 后续 optical frame 一般由 RealSense 驱动发布

当前实测安装外参：

```text
x = -0.20
y = 0
z = 1.215
roll = pitch = yaw = 0
```

查看位置：

[semantic_exploration.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/semantic_exploration.launch.py:56)

这条 TF 决定检测出的椅子相机坐标能否正确变换到车体和地图坐标。即使识别完全正确，外参错误也会造成 Marker 位置错误。

---

### 9. Co-Nav2 → `/navigate_to_pose` → Nav2

归属：

- 目标生成属于 Co-Nav2
- 路径规划、控制属于 Nav2
- 两者通过 ROS 2 Action 连接

原理：

`semantic_explorer` 获取 `map → base_link`，生成八个绝对朝向，但位置保持机器人当前位置不变，然后向 `/navigate_to_pose` 发送目标。Nav2 将其理解为“原地转到指定朝向”。

主要查看：

- 创建 Action 客户端：[semantic_explorer.py:101](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:101)
- 生成八方向扫描目标：[semantic_explorer.py:255](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:255)
- 导航算法辅助函数：[navigation_algorithms.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/navigation_algorithms.py:19)
- 发送导航目标：[semantic_explorer.py:473](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:473)
- Nav2 启动入口：[nav2_navigation.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/nav2_navigation.launch.py:22)
- 速度和代价地图配置：[nav2_open_space.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/nav2_open_space.yaml:4)
- 禁用危险恢复动作的行为树：[open_space_no_recovery.xml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/behavior_trees/open_space_no_recovery.xml:1)

---

### 10. Nav2 → `/cmd_vel` → Ranger → CAN → 电机

归属：

- `/cmd_vel` 的正常运动命令由 Nav2 控制器产生
- `/cmd_vel` 的接收和执行属于 Ranger 底盘
- ROS 到真实控制器之间由 Ranger 驱动和 CAN 连接

原理：

1. Nav2 根据目标姿态、当前 TF 和地图计算速度。
2. 发布 `geometry_msgs/msg/Twist`：
   - `linear.x`：前后速度
   - `angular.z`：旋转角速度
3. Ranger ROS 节点订阅 `/cmd_vel`。
4. Ranger SDK 把速度命令编码成 CAN 帧。
5. 底盘控制器驱动电机。

主要查看：

- Ranger 启动：[ranger_mini_v3.launch.py](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_bringup/launch/ranger_mini_v3.launch.py:1)
- `/cmd_vel` 订阅：[ranger_messenger.cpp](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_base/src/ranger_messenger.cpp:155)
- 底盘状态和 `/odom`：[ranger_messenger.cpp](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_base/src/ranger_messenger.cpp:289)
- Nav2 限速：[nav2_open_space.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/nav2_open_space.yaml:27)

`semantic_explorer` 也能向 `/cmd_vel` 发布零速度，但这主要用于取消导航后的停车保护，不是正常运动控制器。其配置在 [robot.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/robot.yaml:51)。

---

### 11. Ranger `/odom`

归属：

- 属于小车底盘
- 来自底盘反馈速度的积分
- 当前新版中主要用于检查底盘是否运动，不负责发布主定位 TF

原理：

Ranger 驱动从 CAN 读取线速度、角速度、转向状态，根据运动模型积分：

```text
上一时刻位姿 + 速度 × 时间
```

然后发布 `/odom`。

主要代码：

- 获取底盘反馈：[ranger_messenger.cpp:184](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_base/src/ranger_messenger.cpp:184)
- 按运动模式积分：[ranger_messenger.cpp:289](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_base/src/ranger_messenger.cpp:289)
- 发布 `/odom`：[ranger_messenger.cpp:336](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_base/src/ranger_messenger.cpp:336)

要区分：

- `/odom`：Ranger 底盘反馈
- `/Odometry`：FAST_LIO 原始输出，大小写不同
- `/fastlio/odom`：适配成 `odom/base_link` 后的 FAST_LIO 里程计

这是排查时非常容易混淆的三个话题。

---

## 三、旧版 ZMQ 链路

旧版链路是：

```text
Co-NavGPT2
  → ZMQ 文本 speedctl speed|vx|vy|yaw
  → zmq_to_cmd_vel_adapter_safe.py
  → geometry_msgs/Twist
  → /cmd_vel
  → Ranger
```

它属于 Co-Nav 与底盘之间的连接适配层。

主要文件：

[zmq_to_cmd_vel_adapter_safe.py](/home/isee-cdh/zmq_to_cmd_vel_adapter_safe.py:1)

原理：

- ZMQ SUB socket 接收文本命令
- 解析 `vx、vy、yaw`
- 对 `vx`、`yaw` 限幅
- 强制 `linear.y=0`
- 转成 ROS 2 `Twist`
- `real_run` 时才发布 `/cmd_vel`
- 超时自动发布零速度
- 关闭节点时发布零速度

关键代码：

- ZMQ 与 ROS Publisher：[zmq_to_cmd_vel_adapter_safe.py:39](/home/isee-cdh/zmq_to_cmd_vel_adapter_safe.py:39)
- 文本解析和限幅：[zmq_to_cmd_vel_adapter_safe.py:94](/home/isee-cdh/zmq_to_cmd_vel_adapter_safe.py:94)
- 发布 `/cmd_vel`：[zmq_to_cmd_vel_adapter_safe.py:140](/home/isee-cdh/zmq_to_cmd_vel_adapter_safe.py:140)
- watchdog：[zmq_to_cmd_vel_adapter_safe.py:167](/home/isee-cdh/zmq_to_cmd_vel_adapter_safe.py:167)

新版流程已经改为：

```text
Co-Nav2 → ROS 2 Action → Nav2 → /cmd_vel
```

因此运行新版时不应启动这个 ZMQ 适配器。

## 四、建议查看代码的顺序

为了最快理解整个系统，建议按以下顺序读：

1. 总启动和 TF：[semantic_exploration.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/semantic_exploration.launch.py:10)
2. 所有关键参数：[robot.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/robot.yaml:1)
3. Co-Nav 感知与任务状态机：[semantic_explorer.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/semantic_explorer.py:59)
4. FAST_LIO 坐标适配：[fastlio_odom_adapter.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/co_nav2_nav/fastlio_odom_adapter.py:19)
5. 点云转二维地图：[fastlio_mapping_2d.launch.py](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/launch/fastlio_mapping_2d.launch.py:8)
6. FAST_LIO 核心：[laserMapping.cpp](/home/isee-cdh/ws/FAST_LIO_ROS2/src/laserMapping.cpp:809)
7. Nav2 限速与代价地图：[nav2_open_space.yaml](/home/isee-cdh/ws/Co-NavGPT2/co_nav2_nav/config/nav2_open_space.yaml:4)
8. Ranger ROS/CAN：[ranger_messenger.cpp](/home/isee-cdh/agilex_ws/src/ranger_ros2/ranger_base/src/ranger_messenger.cpp:155)

最主要的三份实现代码是：

- Co-Nav：`semantic_explorer.py`
- 定位连接：`fastlio_odom_adapter.py`
- 底盘连接：`ranger_messenger.cpp`

而 launch 和 YAML 主要负责“把哪些模块以什么参数连接起来”。
