- 1.FAST_LIO 初始位置缺失或超出 ±10 m，可能已经发散：最可能原因是：上一次 FAST_LIO 没有退出

- 时间戳问题，大概是tf延迟
- 重复验证时ranger发布odom->base_link：

- [失败] 缺少节点 /livox_lidar_publisher:排查过程为

  ````bash
  cd /home/isee-cdh/ws/conav_scripts
  ./stop_robot.sh
  
  ip -br address
  ping -c 4 192.168.1.122
  
  ./03_livox.sh
  # 排查后
  ros2 node list | grep -Fx /livox_lidar_publisher
  timeout 6 ros2 topic hz /livox/lidar
  timeout 6 ros2 topic hz /livox/imu
  ````

  
