要让完整的 VLM_Nav 运行，目前需要确认三类外参：

   外参                        作用                当前状态
  ━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   LiDAR → IMU                 FAST-LIO 内部点     已有候选值，但仍需最终确认定
                               云与 IMU 融合       义和值
  ──────────────────────────  ──────────────────  ──────────────────────────────
   base_link →                 发布机器人机体到    未通过验证
   utlidar_lidar               雷达的 TF、影响
                               FAST-LIO/障碍地
                               图
  ──────────────────────────  ──────────────────  ──────────────────────────────
   base_link → camera_link     将 RGB-D/VLM 目     尚未标定，是 VLM 启动硬阻塞
                               标投影到机器人坐
                               标系

  具体需要测量：

  1. LiDAR 安装外参
     parent=base_link，child=utlidar_lidar：

     xyz [m]
     rpy [rad]

     当前候选是：

     xyz = [0.28945, 0, -0.046825]
     rpy = [0, 2.8782, 0]

     但该候选此前导致 FAST-LIO 静止漂移过大，目前仍是 calibrated: false。

  2. 相机安装外参
     parent=base_link，child=camera_link：

     xyz [m]
     rpy [rad]

     不能直接复用旧代码中 D455/Livox 的约 0.206 m 外参，因为当前设备是 D435。

  3. LiDAR-IMU 内部外参
     定义为：

     p_imu = R_lidar_to_imu · p_lidar + T_lidar_in_imu

     当前配置使用历史 Co-Nav 值：

     T = [0.171, 0, 0.0908]
     R = I

     但仓库文档还记录了 Unitree L1 SDK 值：

     T = [0.007698, 0.014655, -0.00667]
     R = I

     这两个值的物理含义不能混用，需要最终确定采用哪一种。

  因此，当前最直接的阻塞项是：

  - 重新验证 base_link → utlidar_lidar；
  - 实测 base_link → camera_link；
  - 最终审定 LiDAR→IMU 的定义和值。

  配置位置见：VLM_Nav/config/go2_calibration.yaml:3。VLM 启动明确要求存在
  base_link→camera_link TF：VLM_Nav/launch/go2_vlm.launch.py:27。
