有解决办法。根据报错，直接原因不是底盘机械问题，而是 Nav2 获取到的 TF 落后了约 `1.64 s`。

```
数据时间：1784708651.420
TF 时间： 1784708649.776
延迟：    1.644 s
```

当前 Nav2 允许的 TF 延迟只有 `0.2 s`，超出了约 8 倍。

## 为什么会来回旋转

完整过程是：

```
FAST_LIO 或 SLAM Toolbox 的 TF 更新变慢
                ↓
Nav2 根据约 1.6 秒前的姿态控制
                ↓
转过目标角度后仍认为没有到达
                ↓
反向修正，又再次过调
                ↓
在目标角度附近来回摆动
                ↓
15 秒后 Failed to make progress
```

当前又使用了：

```
progress_checker:
  required_movement_radius: 0.05
  movement_time_allowance: 15.0

general_goal_checker:
  yaw_goal_tolerance: 0.10
```

原地旋转没有产生 `0.05 m` 平移。正常时一次转向能在 15 秒内完成，所以前两次成功；第三次 TF 延迟后无法及时满足角度容差，最终触发失败。

## TF 延迟可能来自哪一段

TF 链路分为：

```
map → odom          SLAM Toolbox 发布
odom → base_link    FAST_LIO 适配器发布
```

常见原因按可能性排序：

1. FAST_LIO 处理积压，`/Odometry` 本身已经延迟。
2. SLAM Toolbox 的 scan 队列积压，`map→odom` 更新不及时。
3. 重复启动 FAST_LIO 或二维建图，产生两个同名发布者。
4. 连续测试后地图增大、回环计算和 YOLO/SAM 同时占用 CPU/GPU。
5. 某次失败后旧 launch 没有完全退出，只残留 `/scan` 或 TF 发布者。
6. `SimpleProgressChecker` 不适合长时间纯旋转。

当前 ROS 节点已经关闭，所以现在无法复现并判断究竟是哪一段延迟；需要在下次运行时测量。

## 下次运行时这样定位

系统启动后、ARM 前分别执行：

```
timeout 10 ros2 topic delay /Odometry
timeout 10 ros2 topic delay /scan
```

再检查两段 TF：

```
timeout 10 ros2 run tf2_ros tf2_monitor odom base_link
timeout 10 ros2 run tf2_ros tf2_monitor map odom
```

检查是否重复启动：

```
ros2 topic info /Odometry
ros2 topic info /cloud_registered_body
ros2 topic info /scan
ros2 topic info /map
```

所有 `Publisher count` 都必须为 `1`。

判断方法：

| 检查结果                        | 延迟来源                        |
| ------------------------------- | ------------------------------- |
| `/Odometry` 已延迟              | FAST_LIO                        |
| `/Odometry` 正常但 `/scan` 延迟 | 点云转换或系统负载              |
| 前两项正常但 `map→odom` 延迟    | SLAM Toolbox                    |
| 发布者数量大于 1                | 重复节点                        |
| TF 都正常但仍报 progress        | progress checker 不适合原地旋转 |

建议指标：

```
/Odometry：最好 < 0.05 s
/scan：最好 < 0.15 s
odom→base_link：< 0.20 s
map→odom：< 0.20 s
```

## 解决顺序

### 1. 先解决重复和积压

每次 ARM 前执行：

```
cd /home/isee-cdh/松灵小车/conav_scripts
./check_all.sh
```

出现失败后不要立即再次 ARM，应先：

```
./stop_robot.sh
```

然后重启 FAST_LIO 和二维建图终端。不要重复打开同一个 launch。

### 2. 降低二维建图负载

空旷小范围测试可以考虑：

```
throttle_scans: 2
do_loop_closing: false
scan_buffer_size: 5
```

这样 SLAM Toolbox 只处理约 5 Hz scan，并暂时关闭不必要的回环计算。

### 3. 改用支持旋转进展的检查器

当前：

```
plugin: nav2_controller::SimpleProgressChecker
```

建议改为：

```
progress_checker:
  plugin: nav2_controller::PoseProgressChecker
  required_movement_radius: 0.05
  required_movement_angle: 0.05
  movement_time_allowance: 20.0
```

这样原地旋转产生的角度变化也会被视为“有进展”，不会只检查平移距离。

但它只能解决 `Failed to make progress`，不能解决 1.64 秒的 TF 滞后。

### 4. 增加 TF 新鲜度安全门

最可靠的代码修正是：Co‑Nav2 每次发送旋转目标前检查 TF 时间。

```
TF 延迟 ≤ 0.20 s：允许发送目标
TF 延迟 > 0.20 s：取消 Nav2、发布零速度、进入 FAILED
```

这样即使 FAST_LIO 或 SLAM 卡顿，小车也不会继续使用旧姿态来回旋转。

### 5. 不建议直接放宽到 2 秒

把 `transform_tolerance` 从 `0.2` 提高到 `2.0` 只会允许 Nav2继续使用旧定位，可能扩大摆动和碰撞风险。应先消除数据积压，再考虑小幅调整到 `0.3 s`。

另外，底盘启动脚本的 CAN 自动判断已完成：DOWN 时自动执行启动命令，UP 时直接继续。